# mini-lm Native App Goal

Last updated: 2026-05-15

この文書は、`mini-lm` を Windows / macOS 両対応のネイティブアプリとして作り直すための厳格なゴール定義である。実装中の判断に迷った場合、この文書を優先する。

## 1. 最終目的

ユーザーが指定したローカル `source` ディレクトリ内の巨大テキスト群だけを根拠に、NotebookLM 的な質問応答を行うデスクトップアプリを作る。

回答は DeepSeek API を使って生成する。ただし、AI に全文を丸投げするのではなく、ローカル側で永続インデックス、全文検索、ベクトル検索、網羅抽出、引用、検証を行い、可能な限り取り逃しと幻覚を減らす。

## 2. 対象プラットフォーム

必須対象:

- Windows 10 / 11
- macOS Apple Silicon
- macOS Intel

配布形態の目標:

- Windows: `.exe` または `.msi`
- macOS: `.app` または `.dmg`

開発中の暫定実行は許容するが、最終ゴールは「ユーザーがアプリとして起動できる」ことであり、ブラウザで `index.html` を直接開く方式は最終形ではない。

## 3. 採用方針

### 3.1 アプリ基盤

第一候補は Tauri とする。

理由:

- Windows / macOS のクロスプラットフォーム配布に向く。
- フロントエンド UI とネイティブ側ファイル処理を分離できる。
- ローカルファイル、SQLite、バックグラウンド処理、永続設定を扱いやすい。
- Electron より軽量な構成を狙える。

ただし、実装中に Tauri が明確な阻害要因になる場合は、Electron などへの変更を許容する。その場合は理由を `README.md` または設計メモに残す。

### 3.2 AI Provider

主 provider は DeepSeek API とする。

DeepSeek API の公式仕様に基づき、少なくとも以下を前提にする。

- OpenAI / Anthropic 互換形式で利用できる。
- OpenAI 互換 base URL は `https://api.deepseek.com`。
- Chat API は `/chat/completions`。
- 主モデル候補は `deepseek-v4-flash` と `deepseek-v4-pro`。
- `deepseek-chat` と `deepseek-reasoner` は 2026-07-24 に廃止予定のため、新規実装の既定値にしない。
- Chat API はステートレスなので、会話履歴や検索結果はアプリ側で管理する。

参考:

- https://api-docs.deepseek.com/
- https://api-docs.deepseek.com/api/create-chat-completion
- https://api-docs.deepseek.com/guides/multi_round_chat

## 4. 非交渉要件

以下は必須であり、未達の場合は「完成」としない。

1. Windows と macOS の両方で使える設計になっている。
2. ローカル `source` ディレクトリを選択でき、その選択が永続保存される。
3. `source` 内の対象テキストファイルを再起動後も再読込できる。
4. ファイル単位で質問対象を選択できる。
5. 10万字超のテキストを前提に、全文を毎回 API に送らない。
6. ローカルに永続インデックスを持つ。
7. 全文検索を持つ。
8. ベクトル検索を持つ。
9. 全文検索とベクトル検索を統合したハイブリッド検索を持つ。
10. 高精度モードでは、検索上位だけでなく、質問に関係しうる全チャンクを段階的に検査する。
11. 回答は `source` 内の情報だけを根拠にする。
12. 根拠がない場合は、推測で答えず「根拠が見つからない」と答える。
13. 回答には出典ファイル名と該当箇所を付ける。
14. 処理中、待機中、リトライ中、停止、完了が UI で明確に分かる。
15. API 制限や通信エラー時に、処理が継続中なのか停止したのかを明示する。
16. 長時間処理をキャンセルできる。
17. 途中結果、抽出メモ、検索結果、最終回答の関係を後から確認できる。
18. API キーは永続保存できるが、平文保存のリスクを UI で明示する。可能なら OS の安全な保存領域を使う。
19. DeepSeek の API エラー、rate limit、context limit を分類して表示する。
20. 実装後に、少なくともローカル実行で主要フローを確認する。

## 5. 対象ファイル

初期対応する拡張子:

- `.txt`
- `.md`
- `.markdown`
- `.csv`
- `.tsv`
- `.json`
- `.log`

必須動作:

- `source` 配下を再帰的に読む。
- 文字コードは UTF-8 を第一候補とする。
- 読めないファイルは全体停止せず、エラー一覧に出す。
- ファイルサイズ、文字数、更新日時、インデックス状態を表示する。
- ファイル更新を検知し、該当ファイルだけ再インデックスできる。

## 6. チャンク設計

巨大テキストを正確に扱うため、チャンクは単純な固定長分割だけにしない。

必須:

- 見出し、条、章、節、項、箇条書き、空行を考慮する。
- 日本語規程文書に多い `第X条`、`第X章`、`第X節`、`（見出し）` を構造として扱う。
- 各チャンクに以下のメタデータを持たせる。
  - file id
  - file path
  - file name
  - chunk id
  - char start
  - char end
  - heading path
  - previous chunk id
  - next chunk id
  - content hash
  - indexed at

目標:

- 通常チャンクは 800 から 1,800 日本語文字程度を基準にする。
- 条文や見出し構造を壊すより、多少長くなることを優先する。
- 回答生成時はヒットチャンクの前後も必要に応じて含める。

## 7. 永続データベース

必須:

- SQLite を使う。
- ファイル、チャンク、全文検索、ベクトル、抽出メモ、質問履歴を永続保存する。
- 再起動してもインデックスを再利用する。
- ファイルの `mtime`、サイズ、hash を比較し、変化したものだけ更新する。

最低限必要なテーブル:

- `documents`
- `chunks`
- `chunk_fts`
- `chunk_embeddings`
- `questions`
- `retrieval_runs`
- `extracted_facts`
- `answers`
- `settings`

## 8. 全文検索

必須:

- SQLite FTS5、または同等以上のローカル全文検索を使う。
- 日本語検索の弱点を補うため、追加の n-gram token index を持つ。
- 完全一致、部分一致、同義語候補、表記揺れを扱う。
- ファイル名、見出し、本文を別々にスコアリングする。

検索要件:

- 短い質問語でも該当箇所を落とさない。
- 例: `扶養手当`、`通勤手当`、`休職` のような短い規程語でヒットする。
- 直接語がない場合でも、関連語展開により候補を拾う。

## 9. ベクトル検索

必須:

- 各チャンクに embedding を作成し、ローカルに永続保存する。
- 質問文も embedding 化して類似検索する。
- 全文検索結果とベクトル検索結果を統合して ranked list を作る。

embedding 実装方針:

- 第一候補はローカル embedding モデルを使う。
- DeepSeek が embedding endpoint を提供しない、または用途に不適な場合でも、DeepSeek chat API に依存せずローカル embedding で成立させる。
- 初回モデル取得が必要な場合は、UI に明示し、以後はキャッシュする。

候補:

- multilingual embedding model
- Japanese 対応 embedding model
- Transformers.js / ONNX Runtime / fastembed 相当

ベクトル保存:

- 小規模では SQLite BLOB 保存とアプリ内 cosine similarity でよい。
- 大規模化を見越し、将来的に sqlite-vec、LanceDB、Qdrant local などへ交換可能な境界を作る。

## 10. ハイブリッド検索

必須:

- 全文検索スコア
- ベクトル類似度
- 完全一致ボーナス
- 見出し一致ボーナス
- ファイル選択フィルタ
- チャンク前後関係
- 重複排除
- ファイル偏りの制御

統合方式:

- Reciprocal Rank Fusion または同等の rank fusion を使う。
- 単一手法に依存しない。
- スコアの内訳を debug UI で確認できる。

## 11. 網羅回答モード

通常回答と高精度網羅回答を分ける。

### 11.1 通常回答

目的:

- 速く回答する。
- 上位検索結果と近傍チャンクを使う。

必須:

- 回答根拠を表示する。
- 根拠不足の場合は不足と答える。

### 11.2 高精度網羅回答

目的:

- 時間がかかってもよいので、取り逃しを最小化する。

必須 pipeline:

1. 質問を解析し、検索語、同義語、表記揺れ、観点を生成する。
2. 選択ファイル全体を構造単位で走査対象にする。
3. 全文検索、ベクトル検索、n-gram 検索で候補を広く集める。
4. 候補だけでなく、関連可能性のある全チャンクを batch 単位で分類する。
5. 各 batch から「質問に関係する事実」だけを抽出する。
6. 抽出事実には必ず source file / chunk id / char range を付ける。
7. 抽出事実を重複統合する。
8. 重要な根拠チャンクの前後を再確認する。
9. 最終回答を作る。
10. 最終回答の各主張が抽出事実に支えられているか検査する。
11. 支えられない主張は削除、または「根拠不十分」に変更する。

禁止:

- 検索上位だけを見て「網羅」と呼ぶこと。
- 出典なしの断定。
- 一般知識による補完。
- DeepSeek の長コンテキストに依存して全文投入すること。

## 12. 回答品質基準

回答は以下を満たす。

- 質問に直接答える。
- `source` 内にある情報だけを使う。
- 条件、例外、対象者、金額、期間、手続き、権限者などを落とさない。
- 複数文書にまたがる場合は、文書ごとの差分を明示する。
- 矛盾がある場合は、矛盾として示す。
- 情報が見つからない場合は、見つからないと明示する。
- 出典を本文末尾または段落ごとに示す。
- 「おそらく」「一般的には」など source 外推測をしない。

## 13. 正確性ガード

必須:

- 回答生成前に抽出事実リストを作る。
- 回答生成後に claim check を行う。
- 根拠チャンクに存在しない主張を検出する。
- 出典のない回答を UI 上で警告する。
- `temperature` は低めを既定にする。
- JSON 抽出が必要な工程では JSON mode または堅牢な JSON 修復処理を使う。

合格基準:

- 最終回答の主要主張には、対応する出典がある。
- 出典を開くと、該当テキストに到達できる。
- 根拠なしの一般論が混ざらない。

## 14. UI 要件

必須画面:

- 設定
- source ディレクトリ管理
- ファイル一覧
- インデックス状態
- チャット
- 検索結果 / 出典
- 高精度処理ログ
- エラー履歴

必須 UI 表示:

- 現在の処理段階
- 進捗件数
- 経過時間
- 推定残り時間が出せる場合は表示
- API 待機中
- API retry 中
- API error による停止
- ユーザーキャンセル
- インデックス作成中
- インデックス完了
- 再インデックス必要

UX 要件:

- 長時間処理中に無反応に見えないこと。
- 60秒以上の待機が必要な場合は、自動で黙って待ち続けない。
- 続行、停止、後で再開の選択肢を出す。
- 処理済みの中間結果を破棄しない。

## 15. DeepSeek API 要件

必須:

- API key を設定できる。
- `deepseek-v4-flash` と `deepseek-v4-pro` を選べる。
- thinking mode の enabled / disabled を選べる。
- reasoning effort を指定できる範囲で選べる。
- streaming response に対応する。
- rate limit / context limit / auth error / server error を区別する。
- request / response の token usage が取れる場合は保存・表示する。
- API 呼び出しログは秘密情報を含まない形で保存する。

既定値:

- 通常回答: `deepseek-v4-flash`
- 高精度最終合成: `deepseek-v4-pro` を選べるが、コスト重視なら `deepseek-v4-flash`
- temperature: 0.1 から 0.3

## 16. セキュリティとプライバシー

必須:

- source の全文を不要に外部送信しない。
- API に送るのは、検索・抽出で必要になった最小限の本文に限定する。
- API key をログに出さない。
- API key を画面表示するときは既定でマスクする。
- 永続保存時は OS keychain / credential store の利用を第一候補とする。
- 平文保存しかできない場合は、明確に警告する。

## 17. パフォーマンス目標

初期対象:

- 数MBから数十MBのテキスト
- 10万字超のファイル
- 複数ファイル

目標:

- 変更なしの再起動後は再インデックスなしで使える。
- 1ファイル更新時に全ファイルを再処理しない。
- 通常検索は数秒以内を目指す。
- 高精度網羅は時間がかかってもよいが、進捗と中間結果を表示する。

## 18. 再開性

必須:

- 高精度網羅処理の run id を保存する。
- batch ごとの処理結果を保存する。
- エラーや中断後、完了済み batch を再利用できる。
- 同じ質問、同じ対象ファイル、同じ index version の場合は再利用可能な抽出結果を使う。

## 19. ログとデバッグ

必須:

- インデックスログ
- 検索ログ
- API 呼び出し状態
- rate limit 待機ログ
- batch 抽出ログ
- claim check ログ

ログには以下を含める。

- timestamp
- operation
- status
- selected files
- chunk count
- elapsed time
- error class
- retry count

ログには API key を含めない。

## 20. テスト・検証基準

「完成」と呼ぶための最低検証:

1. アプリがローカルで起動する。
2. source ディレクトリを選択し、再起動後も保持される。
3. 既存の `source` 内テキストを読み込める。
4. ファイル選択を切り替えられる。
5. インデックスを作成できる。
6. 再起動後にインデックスが残る。
7. 短い語句で全文検索がヒットする。
8. embedding が作成され、ベクトル検索が動く。
9. ハイブリッド検索結果に出典が付く。
10. 通常回答が DeepSeek から返る。
11. 高精度網羅回答が batch 進捗を表示しながら動く。
12. API エラー時に停止状態が分かる。
13. キャンセルできる。
14. 根拠がない質問に対して、推測回答しない。
15. Windows / macOS 両対応でビルド可能な構成になっている。

## 21. Definition of Done

このプロジェクトの完了条件:

- この文書の非交渉要件をすべて満たす。
- 主要フローがローカルで動作確認済み。
- `README.md` に起動方法、設定方法、制限、DeepSeek API key の扱いが書かれている。
- 既知の未対応事項がある場合は、未対応として明示されている。
- 重大な未実装を「できている」と言わない。

## 22. 重要な現実制約

AI の回答は確率的であり、数学的な意味で 100% 正確な回答を常に保証することはできない。

そのため、このプロジェクトでの「100%を目指す」とは、以下を意味する。

- 全文を扱うためのローカル検索基盤を持つ。
- 検索上位だけに依存しない網羅走査を持つ。
- 出典なしの主張を出さない。
- 根拠不足を根拠不足として返す。
- 中間結果と出典を検証可能にする。
- 失敗、制限、停止を隠さない。

この制約を無視して「必ず完全」と表示する UI は作らない。
